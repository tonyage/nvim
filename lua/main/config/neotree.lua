require("ui.themer").highlight("neotree")
return {
  filesystem = {
    hijack_netrw_behavior = "open_default",
  },
  source_selector = {
    winbar = true,
    show_separator_on_edge = false,
  },
}
